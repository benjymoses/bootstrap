import { describe, expect, it } from "vitest";

import { testMessage } from "../src/index.js";

describe("sample tests", () => {
  it("should run this test", () => {
    expect(testMessage).toBe("testMessageForSampleTest");
  });
});
