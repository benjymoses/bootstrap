import { defineConfig } from "vitest/config";

const baseConfig = defineConfig({
  test: {
    coverage: {
      provider: "istanbul",
      enabled: true,
      exclude: ["**/vitest.config.ts", "./vitest.config.ts", "node_modules"],
    },
    exclude: ["**/vitest.config.ts", "node_modules"],
  },
});

export default baseConfig;
